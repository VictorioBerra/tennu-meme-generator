{
  "name": "tennu-meme-generator",
  "version": "1.0.0",
  "description": "A plugin for the tennu IRC framework, generates memes.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "test": "mocha",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.1",
    "request": "^2.83.0",
    "request-promise": "^4.2.2",
    "valid-url": "^1.0.9",
    "yargs": "^11.0.0"
  },
  "peerDependencies": {},
  "devDependencies": {
    "mocha": "^5.0.0",
    "should": "^13.2.1"
  }
}
